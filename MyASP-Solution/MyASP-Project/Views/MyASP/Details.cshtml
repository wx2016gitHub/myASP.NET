@model MyASP_Project.Models.MyASPData

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>My ASP Data</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.UserId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.PhotoID)
        </dt>

        <dd>
            <img src="" width="200px" height="150px">
            @Html.DisplayFor(model => model.PhotoID)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "MyASPList")
</p>

@section scripts {
    <script>
            jQuery.noConflict();
            jQuery(document).ready(function ($) {
                $(window).load(function () {
                    var name, index, str ;
                        name = $("dd").eq(2).text();
                        index = name.indexOf("p");
                        if (index != -1) {
                            str = name.slice(index, index + 3);
                            str = "../../MySource/" + str + ".gif";
                            $("img").attr("src", str);
                        }
                });
            });
    </script>
}
@* @copyright wendy xiao ASP.NET&MVC demo on 6-10-2016 *@
